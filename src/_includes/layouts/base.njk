<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or 'Learn design history through authentic, fully-implemented website demos. From Swiss modernism to digital brutalism.' }}">
  <meta name="keywords" content="design education, web design, design styles, Swiss design, Bauhaus, Brutalism">

  <meta property="og:title" content="{{ title or 'MyWebClass.org' }}">
  <meta property="og:description" content="{{ description or 'Learn design history through real examples' }}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{ page.url }}">

  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{ title or 'MyWebClass.org' }}">
  <meta name="twitter:description" content="{{ description or 'Learn design history through real examples' }}">

  <title>{{ title or 'MyWebClass.org' }}{{ ' | MyWebClass.org' if title }}</title>

  {# GA4 Consent Mode v2 - Initialize BEFORE any gtag calls #}
  <script>
    // Initialize dataLayer and gtag function
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}

    // Set default consent to denied BEFORE any analytics load
    gtag('consent', 'default', {
      'analytics_storage': 'denied',
      'ad_storage': 'denied',
      'ad_user_data': 'denied',
      'ad_personalization': 'denied'
    });

    // GA4 Measurement ID - set to valid ID in production
    // For development/testing, this can be set to 'G-0000000000' or left empty
    window.GA4_MEASUREMENT_ID = '{{ env.GA4_MEASUREMENT_ID or "G-0000000000" }}';
  </script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="{{ '/styles/main.css' | url }}">

  {% block head %}{% endblock %}
</head>
<body class="min-h-screen flex flex-col">
  <a href="#main-content" class="skip-link">Skip to main content</a>

  {% include "components/navigation.njk" %}

  <main id="main-content" class="flex-1">
    {% block content %}{{ content | safe }}{% endblock %}
  </main>

  {% include "components/footer.njk" %}
  {% include "components/cookie-banner.njk" %}
  {% include "components/cookie-modal.njk" %}

  <script src="{{ '/scripts/navigation.js' | url }}" defer></script>
  <script src="{{ '/scripts/cookie-consent.js' | url }}" defer></script>
  {% block scripts %}{% endblock %}
</body>
</html>
