<nav class="bg-white border-b-2 border-black sticky top-0 z-40" aria-label="Main navigation">
  <div class="container-custom">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center gap-3">
        <a href="{{ '/' | url }}" class="flex items-center gap-3 hover:opacity-80 transition-opacity">
          <div class="w-10 h-10 bg-black flex items-center justify-center">
            <span class="text-white font-bold text-lg">M</span>
          </div>
          <div>
            <span class="font-bold text-lg">MyWebClass</span>
            <span class="text-neutral-400">.org</span>
          </div>
        </a>
      </div>

      <div class="hidden md:flex items-center gap-8">
        <a href="{{ '/' | url }}" class="text-sm font-medium text-neutral-500 hover:text-black transition-colors {% if page.url == '/' %}text-black{% endif %}">
          Gallery
        </a>
        <a href="{{ '/articles/' | url }}" class="text-sm font-medium text-neutral-500 hover:text-black transition-colors {% if '/articles/' in page.url %}text-black{% endif %}">
          Articles
        </a>
        <a href="{{ '/about/' | url }}" class="text-sm font-medium text-neutral-500 hover:text-black transition-colors {% if '/about/' in page.url %}text-black{% endif %}">
          About
        </a>
        <a href="{{ '/submit/' | url }}" class="text-sm font-medium text-neutral-500 hover:text-black transition-colors {% if '/submit/' in page.url %}text-black{% endif %}">
          Submit
        </a>
        <a href="{{ '/admin/' | url }}" class="text-sm font-medium text-neutral-500 hover:text-black transition-colors {% if '/admin/' in page.url %}text-black{% endif %}">
          Instructor
        </a>
        <a href="{{ '/submit/' | url }}" class="btn btn-primary text-sm !px-4 !py-2 h-10">
          Submit Demo
        </a>
      </div>

      <button
        id="mobile-menu-button"
        class="md:hidden p-3 text-black"
        aria-label="Toggle mobile menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg id="menu-icon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
        <svg id="close-icon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>

    <div id="mobile-menu" class="hidden md:hidden border-t-2 border-black">
      <div class="py-4 space-y-2">
        <a href="{{ '/' | url }}" class="block text-sm font-medium text-neutral-500 hover:text-black transition-colors py-2 min-h-[44px] flex items-center">
          Gallery
        </a>
        <a href="{{ '/articles/' | url }}" class="block text-sm font-medium text-neutral-500 hover:text-black transition-colors py-2 min-h-[44px] flex items-center">
          Articles
        </a>
        <a href="{{ '/about/' | url }}" class="block text-sm font-medium text-neutral-500 hover:text-black transition-colors py-2 min-h-[44px] flex items-center">
          About
        </a>
        <a href="{{ '/submit/' | url }}" class="block text-sm font-medium text-neutral-500 hover:text-black transition-colors py-2 min-h-[44px] flex items-center">
          Submit
        </a>
        <a href="{{ '/admin/' | url }}" class="block text-sm font-medium text-neutral-500 hover:text-black transition-colors py-2 min-h-[44px] flex items-center">
          Instructor
        </a>
        <a href="{{ '/submit/' | url }}" class="btn btn-primary text-sm w-full block text-center min-h-[44px] flex items-center justify-center">
          Submit Demo
        </a>
      </div>
    </div>
  </div>
</nav>
